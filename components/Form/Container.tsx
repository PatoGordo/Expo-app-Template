import styled from "styled-components/native";
import { styled as nwstyled } from "nativewind";

export const Container = nwstyled(styled.View`
  height: 42px;
  width: 100%;
  background: transparent;
  position: relative;
  opacity: 1;
`);
